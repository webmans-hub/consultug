$(document).ready(function(){		//	форма обратной связи		$('div.formcontactsubmit').click(function(){						var THIS_FORM = $(this).parent();				var name = $(THIS_FORM).find(".field_name").val();		var phone = $(THIS_FORM).find(".field_phone").val();		var emailback = $(THIS_FORM).find(".field_emailback").val();		var message = $(THIS_FORM).find(".field_message").val();		var subject = $(THIS_FORM).find(".field_subject").val();		var emailto = $(THIS_FORM).find(".field_emto").val();		var emailfrom = $(THIS_FORM).find(".field_emfrom").val();		var emailfromtitle = $(THIS_FORM).find(".field_emailfromtitle").val();				var req_input = $(THIS_FORM).find(".required");		var flag = 0;		$.each(req_input, function(index) { //index содержит порядковый номер элемента в выборке (по порядку в html коде)			var THIS_VALUE = $(this).val();			//var reg = /^.{2,300}$/ ;				var reg_email = /^[a-zA-Z0-9\._-]+@[a-zA-Z0-9_\.-]{2,45}\.[a-zA-Z]{2,20}$/ ;									if($(this).is(".field_emailback")){								if (reg_email.test(THIS_VALUE)){					$(this).next().hide();				}				else {					$(this).next().show(); 					$(".contactform .ajaxotvet").hide();										flag++;				}			}			else{								//if (reg.test(THIS_VALUE)){									if (THIS_VALUE){										$(this).next().hide(); 				}				else {					$(this).next().show(); 					$(".contactform .ajaxotvet").hide();					flag++;									}			}		});			if (flag){ return false;}		else {			$("#formcontactajax").load("/mailformcontact.php", {name: name, phone: phone, emailback: emailback, message: message, subject: subject, emailto: emailto, emailfrom:emailfrom, emailfromtitle:emailfromtitle }, function(){				$(THIS_FORM).find(".ajaxotvet").show();				$(THIS_FORM).find("input, textarea").val("");			});		}	}); 		$('div.contactform .errormess').hover(function(){			$(this).hide();	});			//	форма обратной связи  - конец	});